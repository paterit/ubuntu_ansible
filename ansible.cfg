[defaults]
inventory = inventory.ini
# avoid the per-run interpreter discovery warning on macOS/Linux
interpreter_python = auto_silent
# add execution time to the output
callbacks_enabled = ansible.posix.profile_tasks

