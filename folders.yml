- name: Create some folders
  hosts: localhost

  tasks:
    - name: Create a '~/bin' folder
      ansible.builtin.file:
        path: "~/bin"
        state: directory
        mode: "0755"

    - name: Create apps config folder
      ansible.builtin.file:
        path: "~/apps"
        state: directory
        mode: "0755"
